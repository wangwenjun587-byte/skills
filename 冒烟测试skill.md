---
name: skill-test-runner
description: "Skill 可靠性自动化测试框架。覆盖 7 个核心测试维度：输出质量、稳定性、幻觉检测、Prompt 遵循、Tool 调用、上下文记忆、异常处理。当用户需要评估本地 Skill 质量、测试 Skill 是否可靠、生成 Skill 测试报告时触发。"
agent_created: true
---

# Skill Test Runner

## 概述

本技能专门用于对本地已安装的 Skill 进行系统性质量评估。通过 7 个维度的测试，量化 Skill 的
可靠性，输出结构化的测试报告和改进建议，帮助用户判断 Skill 是否适合在生产环境中使用。

---

## 工作流程

### 第一步：读取被测 Skill

1. 确认被测 Skill 名称和路径（默认当前agent存储skill的地址）
2. 读取被测 Skill 类似 `%SKILL.md` 全文，理解其：
   - 声明的功能边界（description）
   - 触发条件
   - 预期的输出格式和工具调用模式
3. 列出已找到的 bundled resources（scripts/、references/、assets/）

### 第二步：设计测试计划

加载 `references/testing_dimensions.md` 以获取各维度的评分标准和测试用例设计方法。

根据被测 Skill 的特性，选择最相关的测试维度并设计具体测试用例：

```
优先测试策略：
  - 代码类 Skill   → 重点测试幻觉检测、Tool调用、输出质量
  - 对话类 Skill   → 重点测试上下文记忆、Prompt遵循、稳定性
  - 数据类 Skill   → 重点测试幻觉检测、输出质量、异常处理
  - 工具集成 Skill → 重点测试Tool调用、异常处理、稳定性
```

**始终测试全部 7 个维度**，但根据 Skill 类型调整每个维度的测试深度。

### 第三步：执行测试

逐一执行各维度的测试用例。每个维度执行后立即记录：
- 测试证据（输出截图/关键内容摘录）
- 发现的问题
- 临时评分（1-5 分）

**评分标准速查**：

| 分数 | 含义 |
|------|------|
| 5 🟢 | 优秀，完全符合预期 |
| 4 🔵 | 良好，轻微不足可接受 |
| 3 🟡 | 一般，有明显改进空间 |
| 2 🟠 | 较差，影响可用性 |
| 1 🔴 | 失败，该维度不可用 |

### 第四步：生成测试报告

使用 `scripts/run_skill_test.py` 生成格式化报告：

```bash
# 查看演示报告（了解输出格式）
python scripts/run_skill_test.py demo

# 查看某维度检查清单
python scripts/run_skill_test.py checklist tool_call

# 列出所有维度
python scripts/run_skill_test.py dims
```

将各维度评分整合后，调用脚本中的 `generate_report()` 函数生成 Markdown 报告。

报告将包含：
- 综合评分和总体评级（A+/A/B/C/D）
- 各维度详情表
- 具体问题清单（按 P0-P3 优先级排序）
- 改进建议

### 第五步：提供改进建议

基于测试结果，针对低分维度（≤3分）给出具体的 SKILL.md 改进建议：

```
常见问题 → 解决方案：
  输出质量低  → 在 SKILL.md 中明确输出格式规范
  幻觉频发   → 添加"验证步骤"指令，引用权威 references 文件
  Prompt遵循差→ 在 SKILL.md 中添加"绝对约束"清单
  Tool调用混乱→ 添加工具选择决策树或优先级说明
  上下文遗忘  → 指示在 SKILL.md 中加入"上下文跟踪"步骤
  异常处理弱  → 添加边界情况处理指南
```

---

## 7 个测试维度说明

各维度详细评分标准、测试用例模板和常见问题见：
`references/testing_dimensions.md`

| 维度 | 关键问题 |
|------|---------|
| 输出质量 | 输出格式是否规范、内容是否完整？ |
| 稳定性 | 同一 Prompt 多次运行结果是否一致？ |
| 幻觉检测 | 返回的事实是否可以被验证？ |
| Prompt 遵循 | 是否严格执行了所有指令（包括负向约束）？ |
| Tool 调用 | 工具选择和参数是否正确高效？ |
| 上下文记忆 | 多轮对话中是否正确保持上下文？ |
| 异常处理 | 面对边界输入是否优雅降级？ |

---

## 资源文件说明

### scripts/run_skill_test.py
- 提供 `generate_report()` 函数生成 Markdown 报告
- 提供 `get_dimension_checklist()` 获取每维度检查清单
- CLI 命令：`demo`（演示）、`checklist`（检查项）、`dims`（维度列表）

### references/testing_dimensions.md
- 7 个维度的详细评分标准
- 各维度的测试用例设计指南
- 常见问题示例和验证方法
- 综合评分计算公式

### assets/test_plan_template.md
- 完整测试执行记录模板
- 测试计划、用例设计、问题清单一体化
- 复制此文件填写具体内容即可开始测试

---

## 输出规范

测试报告必须包含以下部分（顺序固定）：

1. **报告头**：被测 Skill 名、时间、综合得分、总体评级
2. **各维度汇总表**：7 行，含分数和摘要
3. **各维度详情**：依次展开每个维度，含证据和问题列表
4. **优先改进建议**：P0、P1、P2、P3 优先级排序

**总体评级标准**：
- 90-100%：🏆 A+ 可信赖（生产环境可放心使用）
- 75-89%：✅ A  良好可用（可使用，关注低分维度）
- 60-74%：⚠️  B  需要改进（建议改进后再大范围使用）
- 40-59%：🚨 C  风险较高（仅在监督下使用）
- 低于 40%：❌ D  不可靠（重构或放弃）

---

## 注意事项

- 测试前**必须先完整读取**被测 Skill 的 SKILL.md，否则无法设计有针对性的测试用例
- 稳定性测试需要**清空上下文**后重新运行（不能在同一对话中连续运行）
- 幻觉检测的验证步骤需要使用 WebSearch 或实际运行代码来核实
- 对于 Tool 调用维度，重点观察实际的工具调用序列（非模拟）
- 报告保存路径建议：`~/Desktop/skill_tests/` 或用户指定路径
